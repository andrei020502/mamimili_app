import 'package:flutter/material.dart';

class BudgetPlannerTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: const Text('Planner Tab'),
      ),
    );
  }
}
